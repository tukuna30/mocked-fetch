{
  "name": "mocked-fetch",
  "version": "1.0.1",
  "description": "mock fetch api's for quicker app development",
  "main": "dist/mockedFetch.js",
  "types": "dist/mockedFetch.d.ts",
  "scripts": {
    "test": "rm -r ./dist && tsc && node ./dist/spec/test.js"
  },
  "files": [
    "/dist"
  ],
  "keywords": [
    "mock",
    "apis",
    "local",
    "mock",
    "data"
  ],
  "author": "SkillRazr",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/tukuna30/mocked-fetch.git"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2"
  }
}
